# grid-native-items-and-typed-layers

Grid is the source of truth for all structured items. Items store integer cell coordinates, snap on creation/move/resize, and never overlap within their semantic layer. Frame, media, overlay, and annotation layer kinds are auto-assigned by item type. Annotation mode allows free drawing on top.
